# NeuroLearn

Веб-приложение, которое не передаёт знания, а выстраивает навык через практику
до уровня автоматизма.

## Документы

| Файл | Что внутри |
|------|-----------|
| [PRD.md](./PRD.md) | Научные основы, метрики, границы этапов. **Читать первым.** |
| [docs/PLAN.md](./docs/PLAN.md) | Что сделано, что дальше и в каком порядке. **Читать вторым.** |
| [docs/ARCHITECTURE.md](./docs/ARCHITECTURE.md) | Стек, структура директорий, схема БД, правила границ |
| [docs/API.md](./docs/API.md) | Контракты Server Actions и Route Handlers |

## Стек

Next.js 15 (App Router, RSC) · TypeScript strict · TailwindCSS v4 · React Flow ·
Neon Postgres + Drizzle ORM · Auth.js v5 · React Query + Zustand · Vitest.

## Запуск

```bash
npm install
```

Скопировать `.env.example` в `.env.local` и заполнить: строку подключения Neon,
`AUTH_SECRET`, `AUTH_OWNER_PASSWORD`.

Вход — один владелец, регистрации нет. Логин задаётся `AUTH_OWNER_LOGIN`
(по умолчанию `admin`) и может быть обычным именем, не почтой; почта
(`AUTH_OWNER_EMAIL`) необязательна и нужна только строке в `users` и входу
через GitHub. Пароля по умолчанию нет: без `AUTH_OWNER_PASSWORD` вход по
паролю не включается, а `npm run check-env` останавливает сборку, если войти
нечем вообще.

```bash
npm run db:migrate
```

Один раз поставить git-хук, отклоняющий коммит с секретом (`.githooks/pre-commit`;
хук проверяет содержимое индекса, не всё дерево):

```bash
npm run hooks:install
```

```bash
npm run dev
```

Демонстрационное дерево знаний:

```bash
npx tsx scripts/seed-demo.ts
```

## Команды

| Команда | Действие |
|---------|----------|
| `npm run dev` | Дев-сервер |
| `npm run typecheck` | Проверка типов |
| `npm test` | Unit-тесты (Vitest) |
| `npm run e2e` | E2E-проверки против живого приложения (Playwright) |
| `npm run hooks:install` | Включить git-хуки из `.githooks` (скан секретов перед коммитом) |
| `npm run check-env` | Проверить переменные окружения и показать, что выключено |
| `npm run export` | Выгрузить все данные обучения в JSONL (`exports/<дата>/`) |
| `npm run test:providers` | Настоящий вызов к каждому настроенному провайдеру моделей |
| `npm run test:models -- groq:openai/gpt-oss-120b` | Замер конкретных моделей, отчёт в `model-benchmark.json` |
| `npm run test:alert` | Тестовое сообщение в `ALERT_WEBHOOK_URL` |
| `npm run setup:upstash` | Показать базы Upstash и переменные для лимита запросов |
| `npm run fsrs:force-optimize` | Прогон пути переоптимизации весов FSRS с пониженным порогом |
| `npm run generate:pm` | Сгенерировать контент для узлов пути «Продакт-менеджмент» |
| `npm run generate:vapid-keys` | Сгенерировать пару ключей VAPID для Web Push |
| `npm run send-reminders` | Прогнать рассылку напоминаний вручную (тот же путь, что cron) |
| `npm run create-experiment -- --path="Путь"` | Запустить N-of-1 эксперимент (интерливинг 0.3 против 0.5) на узлах пути |
| `npm run experiment-report -- --id=<uuid>` | Честный отчёт по эксперименту: размер выборки, не один процент |
| `npm run db:generate` | Сгенерировать миграцию из схемы |
| `npm run db:migrate` | Применить миграции (через HTTP-эндпоинт Neon) |
| `npx tsx scripts/db-reset.ts` | Сбросить схему `public` (только пустую базу) |

## Тесты

Два уровня, и они проверяют разное.

**Unit (`npm test`, Vitest).** Полностью офлайн: база и вызовы моделей
замоканы. Проверяются правила — оценка ответа, переходы статусов узла,
шаги сборки модуля, контракты роутов. Гонять здесь живую базу означало бы
получать красные прогоны из-за сети, а не из-за кода.

**E2E (`npm run e2e`, Playwright).** Против настоящего дев-сервера и
настоящей базы — сценарии, которые из юнит-тестов не собираются:

1. вход → карта пути → чтение материала узла;
2. полный проход сессии практики до экрана итога;
3. оценка карточки офлайн и её отправка при возврате сети;
4. правка заметки без сети → очередь → сервер (`notebook.spec.ts`);
5. перетаскивание узла → сохранение → «Упорядочить» → «Отменить»
   (`knowledge-map.spec.ts`);
6. подсказки в практике и отключённый тип (`hints.spec.ts`);
7. захват по ⌘K и капсула времени (`notebook-flagship.spec.ts`).

Первый запуск требует браузера:

```bash
npx playwright install chromium
```

Вход выполняется один раз (`e2e/auth.setup.ts`) теми же
`AUTH_OWNER_LOGIN` / `AUTH_OWNER_PASSWORD` из `.env.local`, что и обычный
вход в приложение; сохранённая сессия лежит в `e2e/.auth/` и в репозиторий
не попадает. Сценарии пропускаются (`test.skip`), если в базе нет нужных
данных — пустая очередь повторений это не поломка.

В CI E2E не запускаются: им нужна рабочая база с историей обучения, а
класть её строку подключения в секреты репозитория ради этого не стоит.
CI (`.github/workflows/ci.yml`) гоняет типы, линтер и unit-тесты.

## Ручные настройки

Всё, что нельзя сделать из кода: завести учётку, выпустить ключ, отозвать
старый. Приложение работает и без пунктов 2–5, но каждый из них что-то
выключает — `npm run check-env` печатает, что именно.

### 1. Провайдер моделей (обязательно хотя бы один)

Ключ кладётся **в `.env.local`** — файл в `.gitignore`, в репозиторий не
попадает. Пересылать ключ в переписке или вставлять в чат не нужно ни при
каких обстоятельствах: попавший в переписку ключ считается скомпрометированным
и подлежит отзыву.

| Провайдер | Где выпустить ключ | Переменная | Замечания |
|---|---|---|---|
| Groq | https://console.groq.com/keys | `GROQ_API_KEY` | Бесплатный тариф, самый быстрый ответ (~1 с). Структурированный вывод держит только линейка `gpt-oss` |
| Cerebras | https://cloud.cerebras.ai/platform/apikeys | `CEREBRAS_API_KEY` | Самый быстрый инференс, но **счёт нужно активировать**: иначе `Payment Required` на любую модель, хотя список моделей отдаётся |
| Mistral | https://console.mistral.ai/api-keys | `MISTRAL_API_KEY` | Бесплатный тариф, все `mistral-*` проверены |
| OpenRouter | https://openrouter.ai/keys | `OPENROUTER_API_KEY` | Бесплатные модели с суффиксом `:free`, медленнее остальных |
| DeepSeek | https://platform.deepseek.com/api_keys | `DEEPSEEK_API_KEY` | **Платный**, с предоплатой: без пополнения счёта отвечает `Insufficient Balance` |
| Together AI | https://api.together.ai/settings/api-keys | `TOGETHER_API_KEY` | Не проверялся: ключ, который был под рукой, отвергался как недействительный |
| Google AI Studio | https://aistudio.google.com/apikey | `GOOGLE_GENERATIVE_AI_API_KEY` | Бесплатный тариф Gemini, в ряде стран недоступен |

`AI_PROVIDER` задавать не обязательно — основным станет первый провайдер
с ключом, остальные автоматически станут резервом. После настройки:

```bash
npm run test:providers
```

### 2. Прокси (только если интернет идёт через него)

Node не читает `HTTP_PROXY`/`HTTPS_PROXY` сам, в отличие от curl, git и npm.
Приложение включает их поддержку явно (`src/lib/net/proxy.ts`), но сами
переменные должны быть в окружении. Без этого запросы к провайдерам уходят
напрямую и возвращают 403 — выглядит как блокировка на их стороне, хотя дело
в своей же настройке.

### 3. Лимит запросов (Upstash Redis)

Без него AI-роуты в production отвечают ошибкой — это сделано намеренно, чтобы
выключенная защита не выглядела работающей.

1. Завести бесплатную базу: https://console.upstash.com/redis
2. Скопировать со страницы базы `UPSTASH_REDIS_REST_URL` и `UPSTASH_REDIS_REST_TOKEN`.
3. Положить обе в `.env.local` и в переменные окружения Vercel.

Второй и третий шаги умеет делать `npm run setup:upstash` — для этого нужны
`UPSTASH_EMAIL` (почта аккаунта) и `UPSTASH_MANAGEMENT_API_KEY` (Account →
Management API, это не ключ базы). Без аргументов скрипт только читает: покажет
базы аккаунта и напечатает готовые строки. Создать базу — `-- --create <имя>`,
отдельным флагом, потому что это заведение ресурса в вашем аккаунте.

Если лимит не нужен осознанно (установка на одного человека) — вместо этого
`RATE_LIMIT_DISABLED=1`.

### 4. Оповещения о критичных ошибках

`ALERT_WEBHOOK_URL` — любой адрес, принимающий POST с JSON `{ text }`:

- Slack: входящий вебхук, https://api.slack.com/messaging/webhooks
- Telegram: `https://api.telegram.org/bot<токен>/sendMessage?chat_id=<id>&text=`

Проверка — `npm run test:alert`.

### 5. Секрет для cron

`CRON_SECRET` обязателен в production: без него `/api/cron/*` отвечает 500.
Значение генерируется так же, как `AUTH_SECRET`:

```bash
node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
```

На Vercel переменную достаточно задать в настройках проекта — заголовок
`Authorization: Bearer <секрет>` платформа подставляет своим запросам сама.

### 6. Ротация секретов

Ключ, который хоть раз попал в переписку, лог или скриншот, считается
скомпрометированным. Порядок один и тот же для всех:

1. В панели провайдера выпустить новый ключ.
2. Заменить значение в `.env.local` и в переменных окружения Vercel.
3. Удалить старый ключ в панели — именно удалить, а не оставить «на всякий случай».
4. Проверить: `npm run check-env`, затем `npm run test:providers`.

Отдельные случаи:

- **Пароль базы Neon** — Neon Console, роль базы, «Reset password». Затем
  обновить `DATABASE_URL` в обоих местах.
- **`AUTH_SECRET`** — смена разлогинивает все сессии, это нормально.
- **`VERCEL_OIDC_TOKEN`** в `.env.local` ротировать не нужно: он короткоживущий
  и перевыпускается командой `vercel env pull`.

### 7. Резервные копии данных обучения

Единственная невосстановимая часть проекта — история практики. Код лежит в
git, контент модулей перегенерируется вызовом модели, а `user_responses`,
`review_logs` и `reflections` копятся месяцами и существуют в одном
экземпляре внутри Neon. Ошибочный `db-reset`, случайный `DROP`, истёкший
бесплатный проект Neon — и обучение начинается с нуля.

```bash
npm run export
```

Кладёт по файлу JSONL на таблицу в `exports/<дата>/` плюс `manifest.json` со
счётчиками и списком применённых миграций. Каталог `exports/` в `.gitignore`:
это персональная телеметрия, ей не место в репозитории — храните копию там,
где лежат остальные личные архивы.

Полезные варианты:

```bash
npm run export -- --out D:/backups/neurolearn
npm run export -- --tables review_logs,user_responses
```

Второй вариант — готовый вход для `fsrs-optimizer` (см. раздел про
переоптимизацию весов): отдельная выгрузка под него больше не нужна.

Разумная частота — раз в неделю и обязательно перед любой операцией со схемой
(`db:push`, `db:migrate`, `db-reset.ts`). Обратной загрузки намеренно нет:
восстановление в живую базу слишком опасно, чтобы запускаться той же
командой, что и безобидная выгрузка.

### 8. Push-напоминания об отложенных повторениях

Без этого шага кнопка «Напоминать о повторениях» на экране очереди
отключает сама себя — не притворяется рабочей.

```bash
npm run generate:vapid-keys
```

Выведет четыре значения для `.env.local`: `VAPID_PUBLIC_KEY`,
`VAPID_PRIVATE_KEY`, `VAPID_SUBJECT` (свой `mailto:`) и
`NEXT_PUBLIC_VAPID_PUBLIC_KEY` (тот же публичный ключ, но виден в браузере —
он нужен клиенту при подписке). После этого кнопка на `/review` предложит
подписаться, а `api/cron/send-reminders` (раз в сутки, `vercel.json`) будет
слать напоминание, только если у пользователя реально есть просроченные
карточки.

Проверить рассылку вручную, не дожидаясь cron:

```bash
npm run send-reminders
```

## Состояние

**Этап 0 — готов.** PRD, схема БД, структура директорий, API-контракты.

**Этап 1 — готов.** Каркас Next.js, Drizzle + Neon, аутентификация, CRUD путей
и узлов, интерактивная карта знаний на React Flow.

**Этап 2 — работает.** Генерация модуля тремя шагами с сохранением каждого,
чтение материала (`/paths/[pathId]/nodes/[nodeId]`), тьютор, импорт источников
с расшифровкой аудио в браузере.

**Этап 3 — работает.** Движок практики с интерливингом, FSRS-повторения,
офлайн-очередь оценок. Проверка ответов идёт без обращения к моделям, поэтому
практика доступна и когда провайдер недоступен.

**Этап 4 — работает.** Метакогнитивный дневник, проекты с защитой в песочнице
и аналитика, где расхождение самооценки с фактической точностью показано
отдельной метрикой.

**Международный релиз (фазы W0–W9) — работает.**

- **W0.** Внешний трекинг ошибок (Sentry-совместимый, без зависимостей),
  подсветка активного пункта меню, управление push-устройствами, полный набор
  правил `jsx-a11y` в линтере, откат миграций (`npm run db:rollback`).
- **W1.** Рабочая тетрадь: заметки заякорены на узлы и сессии, типизированные
  связи, полнотекстовый поиск, офлайн-очередь с конфликтными копиями, экспорт
  в Markdown/Obsidian.
- **W2.** Детерминированная раскладка карты, кнопка «Упорядочить» с четырьмя
  режимами группировки, отмена одним шагом, воркер для больших графов, слой
  «Заметки».
- **W3.** Умные подсказки в практике: шесть правил, журнал срабатываний для
  будущей настройки порогов, отключение по типам. Плюс ядро i18n (ru/en/es).
- **W4.** Живые заметки, капсулы времени, захват по ⌘K и через «Поделиться»,
  пайплайны «идея → эксперимент» и «вопрос → тьютор», реестр непонимания.
- **W5.** Вводный тур, глоссарий, виджет «Сегодня», раздел «Как это работает».
- **W6.** Дизайн-система «Сдержанный космос» с проверкой инварианта
  «цвет = данные» тестом.
- **W7.** Локализация интерфейса, мобильный UX, развёрнутая аналитика
  (тренд, тепловая карта, портрет, календарь) и выгрузка CSV.
- **W8.** AI-слой тетради за флагом: гибридный поиск через RRF, недельный
  итог, детектор противоречий. Выключен по умолчанию; без него тетрадь
  функциональна полностью.
- **W9.** Календарная лента `.ics`, категории уведомлений с бюджетом тишины,
  лендинг.

Отложено осознанно: PDF-экспорт тетради через puppeteer (потребовал бы
зависимости с загрузкой браузера), синхронизация календарей через OAuth
(Фаза 2, решение владельца), 3D-режим карты, коллаборация, 2FA/SSO,
публичный API и LMS-экспорт.

## Требования к окружению

TypeScript зафиксирован на 6.x: Next.js 15 не поддерживает нативный компилятор
TypeScript 7 (нет JavaScript-компилятора API, который нужен сборке).
