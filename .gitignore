node_modules/
.next/
out/
build/
dist/

# Секреты — никогда в репозиторий
.env
.env.local
.env*.local

.DS_Store
*.pem
npm-debug.log*
yarn-error.log*
.pnpm-debug.log*

coverage/
# Отчёт `npm run test:models` — снимок состояния апстримов на момент запуска,
# в репозитории от него только конфликты.
model-benchmark.json
.vercel
.turbo
next-env.d.ts
tsconfig.tsbuildinfo
.env*

# Лог фонового прогона генерации контента.
generate-pm.log

# Выгрузки личных данных обучения (`npm run export`).
# В репозиторий не кладём: это персональная телеметрия, а не код.
exports/

# Playwright: сохранённая сессия входа и артефакты прогонов.
e2e/.auth/
playwright-report/
test-results/
