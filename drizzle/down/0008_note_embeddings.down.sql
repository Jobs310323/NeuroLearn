-- Откат 0008_note_embeddings.
-- Векторы теряются целиком, но восстановимы: они считаются заново локальной
-- моделью в браузере из самих заметок, а заметки эта миграция не трогает.
DROP TABLE IF EXISTS "note_embeddings";
